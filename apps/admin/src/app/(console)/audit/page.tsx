"use client";
import { FormEvent, useEffect, useState } from "react";import { PageHeader } from "@/components/page-header";import { LoadingBlock } from "@/components/feedback";import { Pagination } from "@/components/pagination";import { apiRequest } from "@/lib/api";
export default function AuditPage(){const[items,setItems]=useState<any[]>([]);const[loading,setLoading]=useState(true);const[query,setQuery]=useState('');const[page,setPage]=useState(1);const[pages,setPages]=useState(1);async function load(p=page){setLoading(true);const q=new URLSearchParams({page:String(p),limit:'40'});if(query)q.set('query',query);try{const r=await apiRequest<{data:any[];meta:{totalPages:number}}>(`/api/v1/admin/console/audit?${q}`);setItems(r.data);setPages(r.meta.totalPages);setPage(p)}finally{setLoading(false)}}useEffect(()=>{void load(1)},[]);async function search(e:FormEvent){e.preventDefault();await load(1)}return <><PageHeader title="Global audit trail" subtitle="Cross-workspace administrative and product activity."/><div className="card"><div className="card-body border-bottom"><form className="d-flex gap-2" onSubmit={search}><input className="form-control" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search action, entity type or ID"/><button className="btn btn-primary">Search</button></form></div>{loading?<div className="card-body"><LoadingBlock/></div>:<div className="table-responsive"><table className="table mb-0"><thead><tr><th>Time</th><th>Action</th><th>Actor</th><th>Workspace</th><th>Entity</th><th>IP</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{new Date(x.createdAt).toLocaleString()}</td><td><code>{x.action}</code></td><td>{x.actor?.email??x.actorId??'System'}</td><td>{x.workspace?.name??'Platform'}</td><td>{x.entityType}<div className="small font-monospace text-secondary">{x.entityId??'—'}</div></td><td>{x.ipAddress??'—'}</td></tr>)}</tbody></table></div>}<Pagination page={page} totalPages={pages} onChange={p=>void load(p)}/></div></>}
