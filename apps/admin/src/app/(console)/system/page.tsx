"use client";
import { useEffect, useState } from "react";import { PageHeader } from "@/components/page-header";import { LoadingBlock } from "@/components/feedback";import { apiRequest } from "@/lib/api";import { formatBytes } from "@/lib/billing-format";
export default function SystemPage(){const[data,setData]=useState<any>(null);const[error,setError]=useState('');useEffect(()=>{void apiRequest<{data:any}>("/api/v1/admin/console/system").then(r=>setData(r.data)).catch(e=>setError((e as Error).message))},[]);return <><PageHeader title="System health" subtitle="Safe runtime, database, storage and feature-configuration diagnostics."/>{error&&<div className="alert alert-danger">{error}</div>}{!data?<LoadingBlock/>:<div className="row g-4"><div className="col-xl-7"><div className="card mb-4"><div className="card-header d-flex align-items-center gap-2"><span className="admin-health-dot"/><strong>Platform healthy</strong></div><div className="table-responsive"><table className="table mb-0"><tbody><tr><th>Database</th><td>{data.database.connected?'Connected':'Unavailable'}</td></tr><tr><th>Server time</th><td>{new Date(data.database.serverTime).toLocaleString()}</td></tr><tr><th>Latest migration</th><td className="font-monospace">{data.database.latestMigration?.migration_name??'Unavailable'}</td></tr><tr><th>Storage</th><td><pre className="admin-json mb-0">{JSON.stringify(data.storage,null,2)}</pre></td></tr></tbody></table></div></div><div className="card"><div className="card-header"><strong>Runtime</strong></div><div className="table-responsive"><table className="table mb-0"><tbody><tr><th>Environment</th><td>{data.runtime.environment}</td></tr><tr><th>Node.js</th><td>{data.runtime.nodeVersion}</td></tr><tr><th>Uptime</th><td>{data.runtime.uptimeSeconds}s</td></tr><tr><th>RSS memory</th><td>{formatBytes(data.runtime.memory.rssBytes)}</td></tr><tr><th>Heap</th><td>{formatBytes(data.runtime.memory.heapUsedBytes)} / {formatBytes(data.runtime.memory.heapTotalBytes)}</td></tr><tr><th>API diagnostic time</th><td>{data.responseTimeMs}ms</td></tr></tbody></table></div></div></div><div className="col-xl-5"><div className="card mb-4"><div className="card-header"><strong>Feature flags</strong></div><div className="list-group list-group-flush">{Object.entries(data.features).map(([key,value])=><div className="list-group-item d-flex justify-content-between" key={key}><span>{key}</span><strong>{String(value)}</strong></div>)}</div></div><div className="card"><div className="card-header"><strong>Configured administrators</strong></div><div className="list-group list-group-flush">{data.administrators.map((a:any)=><div className="list-group-item" key={a.id}><strong>{a.name}</strong><div className="small text-secondary">{a.email} · {a.status}</div></div>)}</div></div></div></div>}</>}
