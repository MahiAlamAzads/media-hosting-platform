import { access, readFile } from "node:fs/promises"; import path from "node:path"; const root=process.cwd(),m=JSON.parse(await readFile(path.join(root,"module-architecture.json"),"utf8")),fail=[]; for(const mod of m.modules){const b=path.join(root,"src/modules",mod.directory);for(const l of m.requiredLayers){const f=path.join(b,`${mod.stem}.${l}.ts`);try{await access(f)}catch{fail.push(`${mod.name}: missing ${l}`)}}for(const l of ["route","controller"]){try{const s=await readFile(path.join(b,`${mod.stem}.${l}.ts`),"utf8");if(s.includes("@media/database")||/\bprisma\./.test(s))fail.push(`${mod.name}: ${l} accesses Prisma`)}catch{}}} if(fail.length){console.error(fail.join("\n"));process.exit(1)} console.log(`PASS: ${m.modules.length} modules satisfy architecture.`);
